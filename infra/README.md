# infra/

This directory contains operator-facing deployment scaffolding for the **chalk hosted service** running on a Digital Ocean droplet (Caddy + systemd + DO Managed Postgres). It is internal to the team operating the hosted offering. OSS self-hosters do not need anything in this directory; the guidance in `docs/deployment.md` continues to apply to them.
