# chalk-hosted

Hosted multi-tenant runtime for the chalk hosted service. Self-hosters running
their own chalk instance do NOT need this crate; use the OSS `chalk` binary
instead. This crate is for the operator running chalk.app or similar.
